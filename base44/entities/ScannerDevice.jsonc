{
  "name": "ScannerDevice",
  "type": "object",
  "properties": {
    "deviceName": {
      "type": "string"
    },
    "deviceType": {
      "type": "string",
      "enum": [
        "Desktop Camera",
        "Tablet Camera",
        "Mobile Camera",
        "Dedicated Scanner",
        "Webcam",
        "IP Camera",
        "Bluetooth Camera"
      ],
      "default": "Desktop Camera"
    },
    "deviceId": {
      "type": "string"
    },
    "streamUrl": {
      "type": "string",
      "description": "Live feed URL for IP cameras (MJPEG/HTTP snapshot/HLS). Browsers cannot play RTSP directly \u2014 use an MJPEG or HTTP snapshot URL."
    },
    "bluetoothId": {
      "type": "string",
      "description": "Web Bluetooth device id/name when connected via Bluetooth"
    },
    "location": {
      "type": "string"
    },
    "campus": {
      "type": "string"
    },
    "assignedBuilding": {
      "type": "string"
    },
    "assignedRoom": {
      "type": "string"
    },
    "ipAddress": {
      "type": "string"
    },
    "status": {
      "type": "string",
      "enum": [
        "Online",
        "Offline",
        "Maintenance",
        "Disabled"
      ],
      "default": "Online"
    },
    "lastHeartbeat": {
      "type": "string",
      "format": "date"
    },
    "registeredDate": {
      "type": "string",
      "format": "date"
    },
    "firmwareVersion": {
      "type": "string"
    },
    "notes": {
      "type": "string"
    }
  },
  "required": [
    "deviceName",
    "deviceId"
  ]
}